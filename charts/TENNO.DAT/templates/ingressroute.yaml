{{- if .Values.ingress.enabled }}
{{- range $name, $route := .Values.ingress.routes }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ $.Release.Name }}-{{ $name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $.Chart.Name }}-{{ $name }}
    app.kubernetes.io/instance: {{ $.Release.Name }}
    app.kubernetes.io/version: {{ $.Chart.AppVersion }}
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
spec:
  entryPoints:
    - websecure
  {{- if $.Values.ingress.tls.enabled }}
  tls:
    certResolver: {{ $.Values.ingress.tls.certResolver }}
  {{- end }}
  routes:
  - match: Host(`{{ $route.host }}`)
    kind: Rule
    {{- if $.Values.ingress.middlewares }}
    middlewares:
    {{- range $.Values.ingress.middlewares }}
    - name: {{ . }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
    {{- end }}
    {{- end }}
    services:
    - name: {{ $.Release.Name }}-{{ $name }}
      port: {{ $route.port }}
{{- end }}
{{- range $redirect := .Values.ingress.redirects }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ $.Release.Name }}-redirect-{{ $redirect.from | replace "." "-" }}
  namespace: {{ $.Release.Namespace }}
spec:
  redirectRegex:
    regex: "^https?://{{ $redirect.from }}/(.*)"
    replacement: "{{ $redirect.to }}/${1}"
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ $.Release.Name }}-redirect-{{ $redirect.from | replace "." "-" }}
  namespace: {{ $.Release.Namespace }}
spec:
  entryPoints:
    - websecure
  {{- if $.Values.ingress.tls.enabled }}
  tls:
    certResolver: {{ $.Values.ingress.tls.certResolver }}
  {{- end }}
  routes:
  - match: Host(`{{ $redirect.from }}`)
    kind: Rule
    middlewares:
    - name: {{ $.Release.Name }}-redirect-{{ $redirect.from | replace "." "-" }}
    services:
    - name: noop@internal
      kind: TraefikService
{{- end }}
{{- end }}
